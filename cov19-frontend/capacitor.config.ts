import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cov19-frontend',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
